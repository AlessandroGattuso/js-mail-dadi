1 - Create the array
2 - Take the button from html with a query selector
3 - Create a variable called flag to use it to know if the email's user is inside the array
4 - Scroll the array
    4.1 IF the input's element is equal to an element of the array
          - Set flag to true
          - Break the cicle 
5 - IF flag is true
        - Print that the email is valid
    ELSE 
        - Print that the email doesn't exist